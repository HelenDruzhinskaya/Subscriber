<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.myitacademy.samsung.subscriber">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SimpleClicker"></activity>

         <!--
         Используется только для систем ниже API 26, с Android 8.0 данная регистрация игнорируется ОС
         <receiver
            android:name=".My2Receiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="Козьма_Прутков"/>
            </intent-filter>
        </receiver>-->

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>